package newtech;

public class flutter {
    /**
     * 通过skia生成渲染好的图像
     * skia：Skia是一个 2D的绘图引擎库，其前身是一个向量绘图软件，Chrome和 Android均采用 Skia作为绘图引擎。
     * Skia提供了非常友好的 API，并且在图形转换、文字渲染、位图渲染方面都提供了友好、高效的表现。
     * Skia是跨平台的，所以可以被嵌入到 Flutter的 iOS SDK中，而不用去研究 iOS闭源的 Core Graphics / Core Animation。
     *
     * 结构
     * framework：dart实现（与js非常相似）
     * engine：c++实现
     *
     * rn本质是调用原生控件，flutter则是自己渲染控件，与原生无关（flutter只需要一个canvas）。
     */
}
