package datastructure.栈;

public class 栈 {
    /**
     *
     *  首先，栈 (stack) 是一种串列形式的 数据结构。这种数据结构的特点是 后入先出
     *  (LIFO, Last In First Out)，数据只能在串列的一端 (称为：栈顶 top)
     *  进行 推入 (push) 和 弹出 (pop) 操作。根据栈的特点，
     *  很容易的想到可以利用数组，来实现这种数据结构。
     *
     */
}
