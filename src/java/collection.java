package java;

import java.util.*;

public class collection {
    /**
     * 这个类用以说明java集合的知识
     *  1. collection
     *   1. list必须保持元素特定的顺序
     *   2. set不能有重复元素
     *   3. queue保持一个队列(先进先出)的顺序
     *  2. map一组成对的"键值对"对象
     *
     */
    /**
     * 1. HashSet hash即散列，散列即无序
     * 2. LinkedHashSet 有序的散列set
     * 3. TreeSet 树结构的set，有序
     *
     * 1. ArrayList 线程不安全 查询高效（对于linkedlist）
     * 2. Vector 线程安全(stack)
     * 3. LinkedList 双端队列 插入高效（对于arraylist）
     *
     * 1. queue 队列
     * 2. deque 双端队列
     */

    /**
     * 1. hashmap
     * 2. hashtable 线程安全，键值都不能为null
     * 3. linkedHashmap
     * 4. treeMap
     */

}
