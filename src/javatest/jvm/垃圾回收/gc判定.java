package javatest.jvm.垃圾回收;

public class gc判定 {
    /**
     * GC的两种判定方法：引用计数与引用链。
     * 1. 引用计数法：引用计数算法的实现简单，判断效率也很高，在大部分情况下它都是一个不错的算法。
     * 但是Java语言中没有选用引用计数算法来管理内存，其中最主要的一个原因是它很难解决对象之间相互循环引用的问题。
     *
     * 2. 可达性分析（引用链）
     */
}
