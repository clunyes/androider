package sourcecode.glide.api;

public class TransitionOption {
    /**
     * TransitionOption
     *
     * View淡入
     * 与占位符交叉淡入
     *
     * Glide.with(fragment)
     *   .load(url)
     *   .transform(new MultiTransformation(new FitCenter(), new YourCustomTransformation())
     *   .into(imageView);
     *
     *  支持多重变换
     *  支持自定义变换（glide的参数基本都能配置，自定义）
     *
     */
}
