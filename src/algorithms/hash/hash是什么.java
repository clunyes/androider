package algorithms.hash;

public class hash是什么 {
    /**
     * 基本原理就是把任意长度的输入，通过Hash算法变成固定长度的输出。
     * 这个映射的规则就是对应的Hash算法，而原始数据映射后的二进制串就是哈希值。
     * 活动开发中经常使用的MD5和SHA都是历史悠久的Hash算法。
     *
     */
    /**
     * 散列函数的选取原则是：运算尽可能简单；函数的值域必须在散列表的范围内；尽可能使得结点均匀分布，
     * 也就是尽量让不同的关键码具有不同的散列函数值。
     */
}
